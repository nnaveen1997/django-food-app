from django.shortcuts import render
from myfoodapp.models import Image

# Create your views here.
# def home(request):
#     images = Image.objects.all()
#     context = {'images': images}
#     return render(request, )
